name: rpm-update-history
version: 0.1.0

authors:
  - Rodrigo de Avila <me@rda.run>

targets:
  rpm-update-history:
    main: src/rpm-update-history.cr

dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3

crystal: '>= 1.10.0'

license: MIT
