name: rpm-update-history
version: 0.1.0

authors:
  - Rodrigo de Avila <me@rda.run>

targets:
  rpm-update-history:
    main: src/main.cr

dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  tallboy:
    github: epoch/tallboy

crystal: '>= 1.13.1'

license: MIT
